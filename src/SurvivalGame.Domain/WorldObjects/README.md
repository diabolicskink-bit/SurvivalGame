# World Objects

Static map occupants such as walls, trees, furniture, and containers belong here as domain data.

World object definitions describe what an object is. Tile object maps describe where objects are placed. Godot scripts may render and inspect these objects, but should not be the source of truth for object placement or blocking behavior.
