# Local Maps

Local map primitives live here: grid positions, bounds, terrain, chunks, and future local site map storage.

Godot local-map-view scripts should render this state rather than owning it.
