# Gameplay UI

This folder contains gameplay overlay controls such as action buttons, inventory, tooltips, and message-log views.

UI controls should display domain state passed into them and emit user intent. They should not own inventory rules, item definitions, action validation, turn rules, or message generation.
