# Prototype Helpers

This folder is for short-lived Godot-side helpers that make the early prototype run.

Anything here should be considered a candidate to move into `src/SurvivalGame.Domain/` once the matching simulation concept becomes real.
